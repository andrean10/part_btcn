enum ButtonType { elevated, filled, filledTonal, outlined, text }

enum TypeDropDown { menu, dialog, modal, bottomSheet }

enum StatePermission { active, notActive, denied, deniedForever }

enum Role { admin, direktur, finance, user }

enum TypeGoods { req, ret }

enum MethodPayment { cash, transfer, qris }

enum StatusApproval { pending, approved, rejected }

enum StatusPayment { paid, credit }
